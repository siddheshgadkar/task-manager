# Local development datasource — matches docker-compose.yml.
# Not sensitive: local-only default credentials, safe to commit.
spring.datasource.url=jdbc:postgresql://localhost:5432/taskmanager
spring.datasource.username=taskmanager
spring.datasource.password=taskmanager

# Local-only JWT signing secret — fine to commit, never used outside a developer's machine.
# Generated with: openssl rand -base64 32
app.jwt.secret=QWmiJ82u4Fj1eWZ2qI9UVPf1nDpw5FgUBSqdaySlZlQ=
